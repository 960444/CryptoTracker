import { createMuiTheme } from '@material-ui/core/styles'

const theme = createMuiTheme({
    palette: {
        secondary: {
            main: '#fff'
        },
        background: {
            default: '#3f4556',
            paper: '#3f4556'
        },
        text: {
            primary: '#fcfcfc',
            secondary: '#fcfcfc'
        }
    }
  })

  export default theme  